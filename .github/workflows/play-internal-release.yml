name: Release to Play Internal Testing

on:
  push:
    branches:
      - master

concurrency:
  group: play-internal-release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest

    env:
      GARAGE_WEBHOOK_URL: ${{ secrets.GARAGE_WEBHOOK_URL }}
      GATE_WEBHOOK_URL: ${{ secrets.GATE_WEBHOOK_URL }}
      S3_IMAGE_URL: ${{ secrets.S3_IMAGE_URL }}
      S3_MAILBOX_IMAGE_URL: ${{ secrets.S3_MAILBOX_IMAGE_URL }}
      WEATHER_CAMERA_URL: ${{ secrets.WEATHER_CAMERA_URL }}
      DOORBELL_TAKE_IMAGE_URL: ${{ secrets.DOORBELL_TAKE_IMAGE_URL }}
      HA_BASE_URL: ${{ secrets.HA_BASE_URL }}
      HA_TOKEN: ${{ secrets.HA_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Create local.properties
        run: echo "sdk.dir=${ANDROID_SDK_ROOT}" > local.properties

      - name: Decode signing keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > app/pixelspore.keystore

      - name: Create keystore.properties
        run: |
          cat > keystore.properties <<EOF
          storePassword=${{ secrets.KEYSTORE_PASSWORD }}
          keyAlias=${{ secrets.KEY_ALIAS }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          EOF

      - name: Create release notes
        run: |
          mkdir -p whatsnew/en-US
          {
            echo "Automated internal test build."
            echo "Run: ${{ github.run_number }}"
            echo "Commit: ${GITHUB_SHA::7}"
          } > whatsnew/en-US/default.txt

      - name: Build phone and Wear release AABs
        run: |
          VERSION_NAME="2.13.${GITHUB_RUN_NUMBER}"
          PHONE_VERSION_CODE=$((GITHUB_RUN_NUMBER + 16))
          # Wear needs a distinct versionCode in the same Play listing.
          WEAR_VERSION_CODE=$((PHONE_VERSION_CODE + 1000000))

          ./gradlew :app:bundleRelease \
            -PversionCodeOverride=${PHONE_VERSION_CODE} \
            -PversionNameOverride=${VERSION_NAME}

          ./gradlew :wear:bundleRelease \
            -PversionCodeOverride=${WEAR_VERSION_CODE} \
            -PversionNameOverride=${VERSION_NAME}

      - name: Upload phone AAB to Play Internal Testing
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.pixelspore.grefsenveien
          releaseFiles: app/build/outputs/bundle/release/app-release.aab
          track: internal
          status: completed
          whatsNewDirectory: whatsnew

      - name: Upload Wear AAB to Play Wear Internal Testing
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.pixelspore.grefsenveien
          releaseFiles: wear/build/outputs/bundle/release/wear-release.aab
          track: wear:internal
          status: completed
          whatsNewDirectory: whatsnew
