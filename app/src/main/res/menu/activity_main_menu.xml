<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_home"
        android:checkable="true"
        android:checked="true"
        android:icon="@drawable/ic_tab_home_outline"
        android:title="@string/menu_home"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_tour"
        android:checkable="true"
        android:icon="@drawable/ic_tab_tour_outline"
        android:title="@string/menu_tour"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_tour_live"
        android:checkable="true"
        android:icon="@drawable/ic_tab_tour_live_outline"
        android:title="@string/menu_tour_live"
        app:showAsAction="ifRoom" />

</menu>
